spring:
  datasource:
    url: jdbc:mysql://mysql:3306/picture_manager?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root

app:
  file:
    upload-dir: /app/uploads